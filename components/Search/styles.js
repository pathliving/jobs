import {StyleSheet} from 'react-native';
import {SIZES} from '../../constants/theme';

export const styles = StyleSheet.create({
  list: {
    marginHorizontal: SIZES.x12,
    marginBottom: SIZES.x40,
  },
  subtitle: {
    textTransform: 'capitalize',
  },
})